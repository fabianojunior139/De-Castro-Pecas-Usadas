export interface IParts {
  id?: number;
  name: string;
  stock_quantity: number;
  average_price: number;
  year: number;
  observation: string;
  category: string;
  car: string;
  car_brand: string;
  car_model: string;
  user: string;
}
